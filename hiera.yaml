---
version: 5
defaults:
  datadir: "data"
  data_hash: "yaml_data"
hierarchy:
  - name: "Per-node data (yaml version)"
    path: "nodes/%{networking.fqdn}.yaml"
  - name: "Common data"
    path: "common.yaml"
