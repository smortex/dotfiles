---
lookup_options:
  "profile::mail::imap_pass":
    convert_to: "Sensitive"
  "profile::mail::smtp_pass":
    convert_to: "Sensitive"
  keyboard:
    merge: deep

keyboard:
  options:
    - "caps:none"
profile::git::default_settings:
  user:
    name: "Romain Tartière"
  init:
    defaultBranch: "main"
  pull:
    ff: "only"
profile::git::include_settings:
  freebsd:
    path: "~/FreeBSD/"
    settings:
      user:
        email: "romain@FreeBSD.org"
